pipeline {

    agent any

    stages {

        stage("build"){
           steps {

           }
        }
        stage("test"){
            when {
              expression {
                  
              }
            }
            steps{

            }

        }
        stage("deploy"){
         steps {

         }
        }
    }
}