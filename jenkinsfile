pipeline {

    agent any

    stages {

        stage("build"){
           steps {
            //changes
           }
        }
        stage("test"){
            when {
              expression {
                  // TODO:
              }
            }
            steps{

            }

        }
        stage("deploy"){
         steps {

         }
        }
    }
}