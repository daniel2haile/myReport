pipeline {

    agent any

    stages {

        stage("build"){
           steps {
            //changes
           }
        }
        stage("test"){
            when {
              expression {
                  // TODO:
              }
            }
            steps{
              // test for revert----------------------------------------------------------------
            }

        }
        stage("deploy"){
         steps {

         }
        }
    }
}